import CreateStoreForm from '@/components/store/CreateStoreForm/CreateStoreForm';
import React from 'react';

const Become = () => {
	return (
		<div className='flex flex-col min-h-full items-center justify-center'>
			<CreateStoreForm />
		</div>
	);
};

export default Become;
